<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <p><%= t('devise.mailer.hello', :email => @resource.email) %></p>
    <p></p>
    <p><%= t('devise.mailer.locked') %></p>
    <p><%= t('devise.mailer.click_to_unlock') %></p>
    <p><%= link_to t('devise.mailer.unlock'), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>
    <p></p>
    <p><%= t('general.title') %></p>
  </body>
</html>