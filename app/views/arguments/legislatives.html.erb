<section id="arguments-toc">
  <% unless @headings.empty? %>
    <section class="content-bloc">
      <%= render :partial => 'layouts/table_of_content', 
            :locals => { :headings => @headings, :category => 'legislative' } %>
    </section>
  <% end %>
</section>
<section id="arguments-liste2">
  <section class="content-bloc">
    <%= render :partial => 'layouts/index_articles_1col_2_on_3', 
          :locals => { :articles => @articles, :category => 'legislative' } %>
  </section>
</section>